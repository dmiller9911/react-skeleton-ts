import * as breakpoints from './breakpoints';

const narrow = `(min-width: ${breakpoints.sm})`;
const wide = `(min-width: ${breakpoints.md})`;
const full = `(min-width: ${breakpoints.lg})`;

export { narrow, wide, full }
