import { StyleSheet } from 'aphrodite';

export default StyleSheet.create({
  contentHeader: {
    textAlign: 'center'
  },
  heroHeader: {
    color: '#fff',
    fontSize: '2.75rem',
    textAlign: 'center'
  }
});
